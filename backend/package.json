{
  "name": "box-and-beyond-backend",
  "version": "1.0.0",
  "description": "Box & Beyond — receives course-purchase orders from Nextopper and books DTDC shipments for the study material dispatch.",
  "main": "server.js",
  "type": "commonjs",
  "engines": {
    "node": ">=20 <25"
  },
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "check": "node --check server.js && for f in routes/*.js lib/*.js middleware/*.js jobs/*.js; do node --check \"$f\"; done"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.45.4",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "express-rate-limit": "^8.6.1",
    "node-cron": "^4.6.0"
  }
}
